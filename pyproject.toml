[project]
name = "aicos"
version = "0.1.0"
description = "AICOS project skeleton"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-settings>=2.0,<3.0",
    "pyyaml>=6.0,<7.0",
]
